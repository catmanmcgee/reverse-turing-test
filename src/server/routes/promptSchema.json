{
  "$id": "https://example.com/person.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Outcome",
  "type": "object",
  "properties": {
    "dm_response": {
      "type": "string",
      "description": "The dungeon master describing the dialogue, environment, and results.",
      "minLength": 100
    },
    "npc_dialogue": {
      "type": "string",
      "minLength": 100
    },
    "player_roleplay_score_out_of_100": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "description": "How well the player is roleplaying."
    },
    "player_believability_score_out_of_100": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "description": "How believable the player is acting."
    },
    "is_player_dead": {
      "type": "boolean",
      "description": "Indicates if the player is dead."
    },
    "is_player_hurt": {
      "type": "boolean",
      "description": "Indicates if the player is hurt."
    },
    "stable_diffusion_prompt": {
      "type": "string",
      "description": "The stable diffusion prompt describing the situation."
    }
  },
  "required": [
    "dm_response",
    "npc_dialogue",
    "player_roleplay_score_out_of_100",
    "player_believability_score_out_of_100",
    "is_player_dead",
    "is_player_hurt",
    "stable_diffusion_prompt"
  ]
}
